import rollupPopupConfig from './rollup.popup.config';
import rollupManifestConfig from './rollup.manifest.config';
import rollupBackgroundConfig from './rollup.background.config';

export default [
    rollupPopupConfig,
    rollupManifestConfig,
    rollupBackgroundConfig
];
